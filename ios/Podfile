# Uncomment this line to define a global platform for your project
platform :ios, '14.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

# Define flutter_application_path
flutter_application_path = '../'
load File.join(flutter_application_path, 'Flutter', 'podhelper.rb')

target 'Runner' do
  use_frameworks!  # Required for some plugins
  use_modular_headers!

  # Pods for Flutter plugins will be automatically added by podhelper.rb
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    
    # Nếu gặp lỗi Swift version, bật Swift 5
    target.build_configurations.each do |config|
      config.build_settings['SWIFT_VERSION'] = '5.0'
    end
  end
end
